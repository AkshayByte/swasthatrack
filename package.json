{
  "name": "swasthatrack",
  "version": "1.0.0",
  "private": true,
  "description": "Intelligent Healthcare Management & Clinical Workflow Engine",
  "scripts": {
    "dev:backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "dev:frontend": "cd frontend-v2 && npm run dev",
    "dev": "npm --prefix frontend-v2 run dev & python -m uvicorn backend.main:app --reload --port 8000",
    "build": "npm --prefix frontend-v2 run build",
    "test:backend": "cd backend && python -m pytest"
  }
}
